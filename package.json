{
  "name": "job-board",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "seed": "node server/seeds/seeds.js",
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev && npx tailwindcss -i ./src/App.css -o ./dist/output.css --watch\"",
    "install": "cd server && npm i && cd ../client && npm i --production=false",
    "build": "cd client && npm run build",
    "render-build": "npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "dotenv": "^16.4.1"
  }
}
